{
  "name": "github-webhook-lambda",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -j ../lambda.zip index.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
